document.getElementById('calcular').addEventListener('click', function() {
    var valorInicial = parseFloat(document.getElementById('valor-inicial').value);
    var taxaJuros = parseFloat(document.getElementById('taxa-juros').value);
    var valorMensal = parseFloat(document.getElementById('valor-mensal').value);
    var periodo = parseInt(document.getElementById('periodo').value);
  
    var resultado
}
    );

    window.addEventListener('scroll', function() {
        var navbar = document.querySelector('.navbar');
        if (window.scrollY > 0) {
          navbar.classList.add('fixed-top');
        } else {
          navbar.classList.remove('fixed-top');
        }
      });
      